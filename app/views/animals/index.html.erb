<h2>Animals</h2>

<% @animals.each do |animal| %>

  <div>
    <h3><%= animal.name %></h3>
    <p>species: <%= animal.species %></p>
    <p>age: <%= animal.age %> years old</p>
    <%= link_to "see more about #{animal.name}", animal_path(animal.id) %>
  </div>

<% end %>